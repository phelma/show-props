{
  "name": "show-props",
  "version": "1.0.1",
  "description": "A React component that shows you the props that it recieves, useful for debugging",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel index.jsx -o index.js"
  },
  "keywords": [
    "react",
    "json"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-json-tree": "^0.11.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1"
  }
}
